<!DOCTYPE html>
<html>
  <head>
    <title>Notes ‧ <%= yield(:title).presence || "Shaping knowledge" %></title>
    <meta name="description" content="<%= yield(:description).presence || "Hosting ideas and knowledge in constant evolution." %>" />

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <meta name="robots" content="index, follow" />
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="wrapper">
      <header class="box header">
        <%= link_to root_path do %>
          Juan Gallego IV's Notes
        <% end %>

        <div class="actions">
          <% if !logged_in? %>
            <%= link_to "⚙︎", login_path, data: { "turbo-method": :post } %>
          <% else %>
            <%= link_to "⌀", logout_path, data: { "turbo-method": :delete } %>
          <% end %>
        </div>
      </header>
      <section class="box content">
        <% if notice %>
          <p style="color: green"><%= notice %></p>
        <% end %>

        <%= yield %>
      </section>
    </main>
  </body>
</html>
